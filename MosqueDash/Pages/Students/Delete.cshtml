@page "{id:int}"
@model MosqueDash.Pages.Students.DeleteModel
@{
    ViewData["Title"] = "حذف الطالب";
}

<div class="bg-white p-6 md:p-8 rounded-lg shadow-md max-w-3xl mx-auto">
    <h1 class="text-2xl font-bold text-red-700 border-b pb-4 mb-6">تأكيد الحذف</h1>

    <div class="bg-red-50 border-l-4 border-red-500 text-red-700 p-4 rounded-md" role="alert">
        <p class="font-bold">تحذير!</p>
        <p>هل أنت متأكد من أنك تريد حذف الطالب: <strong>@Model.Student.Name</strong>؟</p>
        <p class="mt-2 text-sm">سيتم حذف هذا الطالب وجميع تسجيلاته في الدورات بشكل نهائي. لا يمكن التراجع عن هذا الإجراء.</p>
    </div>

    <dl class="mt-6 border-t border-b divide-y">
        <div class="py-3 sm:grid sm:grid-cols-3 sm:gap-4">
            <dt class="text-sm font-medium text-gray-500">الاسم</dt>
            <dd class="mt-1 text-sm text-gray-900 sm:mt-0 sm:col-span-2">@Model.Student.Name</dd>
        </div>
        <div class="py-3 sm:grid sm:grid-cols-3 sm:gap-4">
            <dt class="text-sm font-medium text-gray-500">العمر</dt>
            <dd class="mt-1 text-sm text-gray-900 sm:mt-0 sm:col-span-2">@Model.Student.Age</dd>
        </div>
        <div class="py-3 sm:grid sm:grid-cols-3 sm:gap-4">
            <dt class="text-sm font-medium text-gray-500">رقم الهاتف</dt>
            <dd class="mt-1 text-sm text-gray-900 sm:mt-0 sm:col-span-2">@Model.Student.PhoneNumber</dd>
        </div>
    </dl>

    <form method="post" class="mt-6">
        <input type="hidden" asp-for="Student.Id" />
        <div class="flex justify-end gap-4">
            <a asp-page="./Index" class="inline-flex items-center px-4 py-2 bg-gray-200 text-gray-800 font-semibold text-sm rounded-md shadow-sm hover:bg-gray-300 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-gray-500 transition-colors">
                إلغاء
            </a>
            <button type="submit" class="inline-flex items-center px-4 py-2 bg-red-600 text-white font-semibold text-sm rounded-md shadow-sm hover:bg-red-700 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-red-500 transition-colors">
                نعم، قم بالحذف
            </button>
        </div>
    </form>
</div>
